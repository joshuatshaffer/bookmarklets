{
  "scripts": {
    "build": "tsc && terser dist/bookmarklets/mouse-trail.js --compress passes=2 --mangle -o dist/bookmarklets/mouse-trail.min.js && terser dist/bookmarklets/search-reddit.js --compress passes=2 --mangle -o dist/bookmarklets/search-reddit.min.js && tsx src/generate.ts"
  },
  "dependencies": {
    "simple-html-template-tag": "0.0.5"
  },
  "devDependencies": {
    "@types/node": "22.7.4",
    "terser": "5.34.1",
    "tsx": "4.19.1",
    "typescript": "5.6.2"
  }
}
